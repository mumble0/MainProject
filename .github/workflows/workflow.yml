name: CI/CD Pipeline

on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Build dotnet
        run: dotnet build  ConsoleApp6/ConsoleApp6/ConsoleApp6.csproj